#Test file

def hello_world(): #Function that returns a phrase
    phrase = str("Hello World!") #The phrase to be returned
    return phrase #Returns the phrase and ends the function
print(hello_world()) #Runs the function and prints the output
